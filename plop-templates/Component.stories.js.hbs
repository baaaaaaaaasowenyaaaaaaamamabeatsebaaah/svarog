import {{pascalCase name}} from './{{pascalCase name}}';

export default {
  title: '{{pascalCase name}}',
};

export const Default = () => {
  const component = new {{pascalCase name}}();
  return component.getElement();
};
